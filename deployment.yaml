apiVersion: v1
kind: Namespace
metadata:
  name: axonai
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: axonai
  namespace: axonai
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: axonai
      version: v1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: axonai
        version: v1
    spec:
      containers:
      - image: axonai/gateway
        imagePullPolicy: IfNotPresent
        name: axonai
        ports:
        - containerPort: 8787
          protocol: TCP
        resources: {}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
---
apiVersion: v1
kind: Service
metadata:
  name: axonai
  namespace: axonai
spec:
  ports:
  - port: 8787
    protocol: TCP
    targetPort: 8787
  selector:
    app: axonai
    version: v1
  sessionAffinity: None
  type: NodePort


